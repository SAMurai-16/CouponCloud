# Flutter Password Validation Updates

Current plan approved.

**Remaining Steps:**
1. [ ] Update _InputField widget in `lib/main.dart` to accept `label` and `validator`
2. [ ] Add `_passwordValidator` function in `lib/auth_flow.dart`
3. [ ] Update LoginScreen password _InputField call in `lib/auth_flow.dart`
4. [ ] Update SignupScreen password _InputField call in `lib/auth_flow.dart`
5. [ ] Test: Run `flutter run` or hot reload to verify validators show messages and helper text displays "Minimum 8 characters required."
6. [ ] Mark complete and cleanup TODO.md

Will update this file after each step.
